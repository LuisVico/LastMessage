{
  "name": "leo-panic-chat",
  "private": true,
  "scripts": {
    "dev": "concurrently npm:dev:vite npm:dev:backend",
    "dev:vite": "vite",
    "dev:backend": "cd backend && npm start",
    "build:css": "tailwindcss -o dist/output.css --minify",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "concurrently": "^8.2.2",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.2.0"
  }
}
